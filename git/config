[core]
  editor = nvim
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  line-numbers = true
  navigate = true
  side-by-side = true
  ; just run: delta --show-syntax-themes
  syntax-theme = "Solarized (light)"
  ; syntax-theme = "Solarized (dark)"
  ; syntax-theme = "OneHalfDark"
  ; syntax-theme = "gruvbox-dark"
  true-color = always
