local M = {}

M.setup = function()
  -- require('mason').setup({
  --   ui = {
  --     icons = {
  --       package_installed = '✓',
  --       package_pending = '➜',
  --       package_uninstalled = '✗'
  --     }
  --   }
  -- })

  -- require('mason-lspconfig').setup({
  --   automatic_installation = true,
  --   ensure_installed = {
  --     'sumneko_lua'
  --   },
  -- })
end

return M
